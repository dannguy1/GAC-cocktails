# Path to cocktail data directory (contains cocktail_menu.json, recipes/, images/)
# Default assumes GAC-Menu is a sibling directory
COCKTAIL_DATA_DIR=../GAC-Menu/cocktails

# Port for the Vite preview (production) server
PORT=8510
